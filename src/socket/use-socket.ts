// ----------------------------------------------------------------------
// Separate file for useSocket hook — Vite Fast Refresh ke liye
// socket-context.tsx mein component (SocketProvider) + hook mix nahi hona chahiye
// ----------------------------------------------------------------------

export { useSocket } from './socket-context';
